# IMPORTANT: For security reason, never ever commit secret values, only keys

apiVersion: v1
kind: Secret
metadata:
  name: '<%=secrets.name%>'
type: Opaque
stringData:
  MY_KEY1: "my value1"
  MY_KEY2: "my value2"