              resources:
                limits:
                  cpu: <%= locals[:cpu][:max] %>
                  memory: <%= locals[:mem][:max] %>
                requests:
                  cpu: <%= locals[:cpu][:min] %>
                  memory: <%= locals[:mem][:min] %>
